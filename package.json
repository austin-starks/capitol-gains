{
  "name": "capitol-gains",
  "version": "0.0.0",
  "private": true,
  "description": "Congressional trading disclosures: a resumable, sharded extraction pipeline with pluggable storage, model and OCR backends.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/austin-starks/capitol-gains.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
