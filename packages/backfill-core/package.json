{
  "name": "@austin-starks/backfill-core",
  "version": "0.1.1",
  "description": "Resumable sharded backfills: content-addressed receipts, cross-process single-flight leases, and progress measured from durable artifacts rather than logs.",
  "license": "MIT",
  "author": "Austin Starks",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/austin-starks/capitol-gains.git",
    "directory": "packages/backfill-core"
  },
  "homepage": "https://github.com/austin-starks/capitol-gains#readme",
  "keywords": ["backfill", "sharding", "idempotent", "s3", "etl", "resumable"],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.716.0"
  }
}
